language: cpp

before_install:
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  - sudo apt-get -qq update
  - sudo apt-get install -y gcc-6 libsdl2-dev libsdl2-image-dev libsdl2-ttf-dev libegl1-mesa-dev
  - mkdir src/build

script:
  - cd src/build
  - cmake ..
  - make

install: true