1. Find a way to have CMAKE get the PATH environement

2. Find a way to tell CMAKE to use the emscripten.cmake file from ~/emscripten.cmake

3. Use those arguments 
cmake -DCMAKE_TOOLCHAIN_FILE=~/Emscripten.cmake -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_FLAGS=-std=c++11 -G "Unix Makefiles" ..

4. Get the emcc to generate an HTML file.
   OR
   Make a dummy project and output to html, copy the HTML page and replace the 
   script link.

5. Fix problems
  a. Tell emscripten about that main loop function.